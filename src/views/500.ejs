<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"><%= (typeof texts !== 'undefined' && texts.site) ? texts.site.name : '公司门户' %></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/"><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.home : '首页' %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about"><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.about : '关于' %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/products"><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.products : '产品' %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/news"><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.news : '新闻' %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contact"><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.contact : '联系' %></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/video-tutorials">
                            <i class="fas fa-play-circle me-1"></i><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.videoTutorials : '视频教程' %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/downloads">
                            <i class="fas fa-download me-1"></i><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.downloads : '下载' %>
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="fas fa-cog"></i> <%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.systemTools : '系统工具' %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="/api-docs" target="_blank">
                                <i class="fas fa-book-open text-info me-2"></i><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.apiDocs : 'API 文档' %>
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/admin">
                                <i class="fas fa-user-shield text-warning me-2"></i><%= (typeof texts !== 'undefined' && texts.nav) ? texts.nav.adminPanel : '管理员' %>
                            </a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="display-1">500</h1>
                <h2>服务器内部错误</h2>
                <p class="lead">抱歉，服务器出现了问题，请稍后再试。</p>
                <a href="/" class="btn btn-primary">返回首页</a>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4">
        <div class="container">
            <p><%= (typeof texts !== 'undefined' && texts.errors) ? texts.errors.copyright : '' %></p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>