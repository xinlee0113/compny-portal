<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageData.title %> - 智云科技</title>
    <meta name="description" content="<%= pageData.description %>">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Highlight.js CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.8.0/styles/github-dark.min.css" rel="stylesheet">
    <!-- 自定义样式 -->
    <link href="/css/style.css" rel="stylesheet">
    
    <style>
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 80px 0;
        }
        
        .blog-stats {
            background: rgba(255,255,255,0.1);
            border-radius: 15px;
            padding: 30px;
            margin-top: 30px;
        }
        
        .stat-item {
            text-align: center;
            padding: 10px;
        }
        
        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: white;
        }
        
        .stat-label {
            font-size: 0.9rem;
            opacity: 0.9;
        }
        
        .article-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            overflow: hidden;
            transition: all 0.3s ease;
            height: 100%;
        }
        
        .article-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }
        
        .article-card.featured {
            border: 2px solid #ffd700;
            position: relative;
        }
        
        .featured-badge {
            position: absolute;
            top: 15px;
            left: 15px;
            background: #ffd700;
            color: #000;
            padding: 5px 15px;
            border-radius: 20px;
            font-weight: bold;
            font-size: 0.8rem;
            z-index: 10;
        }
        
        .article-image {
            height: 200px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            position: relative;
            overflow: hidden;
        }
        
        .article-image::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.3);
        }
        
        .article-image .placeholder-icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: white;
            font-size: 3rem;
            z-index: 5;
        }
        
        .article-meta {
            display: flex;
            align-items: center;
            gap: 15px;
            font-size: 0.85rem;
            color: #6c757d;
            margin-bottom: 15px;
        }
        
        .article-meta span {
            display: flex;
            align-items: center;
            gap: 5px;
        }
        
        .tag {
            display: inline-block;
            background: rgba(102, 126, 234, 0.1);
            color: #667eea;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.75rem;
            text-decoration: none;
            margin: 2px;
            transition: all 0.3s ease;
        }
        
        .tag:hover {
            background: #667eea;
            color: white;
        }
        
        .sidebar-widget {
            background: white;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            padding: 25px;
            margin-bottom: 30px;
        }
        
        .category-list {
            list-style: none;
            padding: 0;
        }
        
        .category-list li {
            padding: 10px 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: between;
            align-items: center;
        }
        
        .category-list li:last-child {
            border-bottom: none;
        }
        
        .category-list a {
            text-decoration: none;
            color: #333;
            flex: 1;
            transition: color 0.3s ease;
        }
        
        .category-list a:hover {
            color: #667eea;
        }
        
        .category-count {
            background: #f8f9fa;
            color: #6c757d;
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 0.8rem;
        }
        
        .tag-cloud {
            display: flex;
            flex-wrap: wrap;
            gap: 5px;
        }
        
        .search-form {
            position: relative;
        }
        
        .search-form input {
            border-radius: 25px;
            padding: 12px 20px 12px 45px;
            border: 2px solid #e9ecef;
            width: 100%;
        }
        
        .search-form input:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        
        .search-form .search-icon {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
        }
        
        .pagination {
            justify-content: center;
            margin-top: 40px;
        }
        
        .pagination .page-link {
            border-radius: 8px;
            margin: 0 2px;
            border: 2px solid #e9ecef;
            color: #667eea;
        }
        
        .pagination .page-link:hover {
            background: #667eea;
            border-color: #667eea;
            color: white;
        }
        
        .pagination .page-link.active {
            background: #667eea;
            border-color: #667eea;
        }
        
        .featured-section {
            background: #f8f9fa;
            padding: 60px 0;
        }
        
        .featured-article {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            transition: all 0.3s ease;
        }
        
        .featured-article:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0,0,0,0.15);
        }
        
        .read-more-btn {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 8px 20px;
            border-radius: 20px;
            text-decoration: none;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            display: inline-block;
        }
        
        .read-more-btn:hover {
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>
<body>
    <!-- 导航栏 -->
    <%- include('../partials/navbar') %>
    
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8">
                    <h1 class="display-4 fw-bold mb-4">技术博客</h1>
                    <p class="lead mb-4">分享车载应用开发的最新技术、实践经验和行业洞察，助力开发者成长</p>
                    
                    <!-- 搜索框 -->
                    <form action="/blog/search" method="GET" class="search-form mb-4">
                        <input type="text" name="q" placeholder="搜索技术文章..." class="form-control">
                        <i class="fas fa-search search-icon"></i>
                    </form>
                </div>
                <div class="col-lg-4">
                    <div class="text-center">
                        <i class="fas fa-blog fa-8x opacity-75"></i>
                    </div>
                </div>
            </div>
            
            <!-- 统计数据 -->
            <div class="blog-stats">
                <div class="row">
                    <div class="col-md-4">
                        <div class="stat-item">
                            <div class="stat-number"><%= pageData.stats.totalArticles %></div>
                            <div class="stat-label">技术文章</div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="stat-item">
                            <div class="stat-number"><%= Math.floor(pageData.stats.totalViews / 1000) %>K+</div>
                            <div class="stat-label">总阅读量</div>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="stat-item">
                            <div class="stat-number"><%= pageData.stats.totalLikes %></div>
                            <div class="stat-label">获得赞数</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- 精选文章 -->
    <% if (pageData.featuredArticles && pageData.featuredArticles.length > 0) { %>
    <section class="featured-section">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-5 fw-bold">精选文章</h2>
                <p class="lead text-muted">编辑推荐的优质技术文章</p>
            </div>
            
            <div class="row g-4">
                <% pageData.featuredArticles.forEach(article => { %>
                <div class="col-lg-4">
                    <div class="featured-article">
                        <div class="article-image">
                            <div class="placeholder-icon">
                                <i class="fas fa-file-alt"></i>
                            </div>
                        </div>
                        <div class="p-4">
                            <div class="article-meta">
                                <span><i class="fas fa-calendar"></i><%= new Date(article.publishDate).toLocaleDateString('zh-CN') %></span>
                                <span><i class="fas fa-clock"></i><%= article.readTime %></span>
                                <span><i class="fas fa-eye"></i><%= article.views %></span>
                            </div>
                            <h5 class="mb-3"><%= article.title %></h5>
                            <p class="text-muted mb-3"><%= article.excerpt %></p>
                            <div class="d-flex justify-content-between align-items-center">
                                <small class="text-muted">by <%= article.author %></small>
                                <a href="/blog/article/<%= article.slug %>" class="read-more-btn">阅读全文</a>
                            </div>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </section>
    <% } %>
    
    <!-- 文章列表 -->
    <section class="py-5">
        <div class="container">
            <div class="row">
                <!-- 主要内容 -->
                <div class="col-lg-8">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h3>最新文章</h3>
                        <span class="text-muted">共 <%= pageData.pagination.totalArticles %> 篇文章</span>
                    </div>
                    
                    <div class="row g-4">
                        <% pageData.articles.forEach(article => { %>
                        <div class="col-md-6">
                            <div class="article-card <%= article.featured ? 'featured' : '' %>">
                                <% if (article.featured) { %>
                                <div class="featured-badge">
                                    <i class="fas fa-star me-1"></i>精选
                                </div>
                                <% } %>
                                
                                <div class="article-image">
                                    <div class="placeholder-icon">
                                        <i class="fas fa-file-alt"></i>
                                    </div>
                                </div>
                                
                                <div class="p-4">
                                    <div class="article-meta">
                                        <span><i class="fas fa-folder"></i><%= article.category %></span>
                                        <span><i class="fas fa-calendar"></i><%= new Date(article.publishDate).toLocaleDateString('zh-CN') %></span>
                                    </div>
                                    
                                    <h5 class="mb-3">
                                        <a href="/blog/article/<%= article.slug %>" class="text-decoration-none text-dark">
                                            <%= article.title %>
                                        </a>
                                    </h5>
                                    
                                    <p class="text-muted mb-3"><%= article.excerpt %></p>
                                    
                                    <div class="mb-3">
                                        <% article.tags.slice(0, 3).forEach(tag => { %>
                                        <a href="/blog/tag/<%= encodeURIComponent(tag) %>" class="tag">#<%= tag %></a>
                                        <% }) %>
                                    </div>
                                    
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="text-muted">
                                            <small><i class="fas fa-clock me-1"></i><%= article.readTime %></small>
                                            <small class="ms-3"><i class="fas fa-eye me-1"></i><%= article.views %></small>
                                            <small class="ms-3"><i class="fas fa-heart me-1"></i><%= article.likes %></small>
                                        </div>
                                        <a href="/blog/article/<%= article.slug %>" class="read-more-btn">阅读全文</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <% }) %>
                    </div>
                    
                    <!-- 分页 -->
                    <% if (pageData.pagination.totalPages > 1) { %>
                    <nav aria-label="博客分页">
                        <ul class="pagination">
                            <% if (pageData.pagination.hasPrev) { %>
                            <li class="page-item">
                                <a class="page-link" href="/blog?page=<%= pageData.pagination.prevPage %>">
                                    <i class="fas fa-chevron-left"></i> 上一页
                                </a>
                            </li>
                            <% } %>
                            
                            <% for (let i = Math.max(1, pageData.pagination.currentPage - 2); i <= Math.min(pageData.pagination.totalPages, pageData.pagination.currentPage + 2); i++) { %>
                            <li class="page-item <%= i === pageData.pagination.currentPage ? 'active' : '' %>">
                                <a class="page-link" href="/blog?page=<%= i %>"><%= i %></a>
                            </li>
                            <% } %>
                            
                            <% if (pageData.pagination.hasNext) { %>
                            <li class="page-item">
                                <a class="page-link" href="/blog?page=<%= pageData.pagination.nextPage %>">
                                    下一页 <i class="fas fa-chevron-right"></i>
                                </a>
                            </li>
                            <% } %>
                        </ul>
                    </nav>
                    <% } %>
                </div>
                
                <!-- 侧边栏 -->
                <div class="col-lg-4">
                    <!-- 文章分类 -->
                    <div class="sidebar-widget">
                        <h5 class="mb-4">文章分类</h5>
                        <ul class="category-list">
                            <% pageData.categories.forEach(category => { %>
                            <li>
                                <a href="/blog/category/<%= encodeURIComponent(category.name) %>">
                                    <%= category.name %>
                                </a>
                                <span class="category-count"><%= category.count %></span>
                            </li>
                            <% }) %>
                        </ul>
                    </div>
                    
                    <!-- 热门标签 -->
                    <div class="sidebar-widget">
                        <h5 class="mb-4">热门标签</h5>
                        <div class="tag-cloud">
                            <% pageData.popularTags.forEach(tag => { %>
                            <a href="/blog/tag/<%= encodeURIComponent(tag.name) %>" 
                               class="tag" 
                               style="font-size: <%= Math.min(1.2, 0.8 + tag.count * 0.1) %>rem;">
                                #<%= tag.name %>
                            </a>
                            <% }) %>
                        </div>
                    </div>
                    
                    <!-- RSS订阅 -->
                    <div class="sidebar-widget text-center">
                        <h5 class="mb-4">订阅我们</h5>
                        <p class="text-muted mb-3">获取最新技术文章推送</p>
                        <a href="/blog/rss" class="btn btn-outline-primary">
                            <i class="fas fa-rss me-2"></i>RSS订阅
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- 页脚 -->
    <%- include('../partials/footer') %>
    
    <!-- Bootstrap JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
    <!-- Highlight.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.8.0/highlight.min.js"></script>
    
    <script>
        // 初始化代码高亮
        document.addEventListener('DOMContentLoaded', function() {
            hljs.highlightAll();
        });
        
        // 搜索表单增强
        document.querySelector('.search-form').addEventListener('submit', function(e) {
            const query = this.querySelector('input[name="q"]').value.trim();
            if (!query) {
                e.preventDefault();
                alert('请输入搜索关键词');
            }
        });
        
        // 文章卡片点击效果
        document.querySelectorAll('.article-card').forEach(card => {
            card.addEventListener('click', function(e) {
                if (!e.target.closest('a')) {
                    const link = this.querySelector('h5 a');
                    if (link) {
                        window.location.href = link.href;
                    }
                }
            });
        });
    </script>
</body>
</html> 