<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= texts.site.title %></title>
    <meta name="description" content="专业的车载系统集成服务，CAN总线、车载网关、云端服务集成解决方案">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        .hero-section {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            color: white;
            padding: 100px 0;
        }
        .integration-card {
            transition: all 0.3s ease;
            border: none;
            height: 100%;
            background: linear-gradient(45deg, #ffffff, #f8f9fa);
        }
        .integration-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
        }
        .protocol-badge {
            background: linear-gradient(45deg, #667eea, #764ba2);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.9rem;
            margin: 2px;
            display: inline-block;
        }
        .feature-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(45deg, #4facfe, #00f2fe);
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 1.5rem;
        }
    </style>
</head>
<body>
    <!-- 导航栏 -->
    <%- include('../partials/navbar') %>

    <!-- 英雄区域 -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8">
                    <h1 class="display-4 fw-bold mb-4"><%= serviceData.hero.title %></h1>
                    <p class="lead mb-4"><%= serviceData.hero.subtitle %></p>
                    <div class="row">
                        <% serviceData.hero.features.forEach(feature => { %>
                        <div class="col-md-3 col-6 mb-3">
                            <div class="d-flex align-items-center">
                                <i class="fas fa-check-circle text-warning me-2"></i>
                                <span><%= feature %></span>
                            </div>
                        </div>
                        <% }) %>
                    </div>
                </div>
                <div class="col-lg-4">
                    <div class="text-center">
                        <i class="fas fa-network-wired text-warning" style="font-size: 8rem; opacity: 0.8;"></i>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- 集成服务 -->
    <section class="py-5">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="display-5 fw-bold">系统集成服务</h2>
                <p class="lead text-muted">专业的车载系统集成解决方案</p>
            </div>
            
            <div class="row">
                <% serviceData.integrationServices.forEach((service, index) => { %>
                <div class="col-lg-4 mb-4">
                    <div class="card integration-card shadow-sm">
                        <div class="card-body p-4 text-center">
                            <div class="feature-icon">
                                <i class="<%= service.icon %>"></i>
                            </div>
                            <h4 class="card-title fw-bold mb-3"><%= service.title %></h4>
                            <p class="card-text text-muted mb-4"><%= service.description %></p>
                            
                            <% if (service.protocols) { %>
                            <h6 class="fw-bold mb-3">支持协议：</h6>
                            <div class="mb-4">
                                <% service.protocols.forEach(protocol => { %>
                                <span class="protocol-badge"><%= protocol %></span>
                                <% }) %>
                            </div>
                            <% } %>
                            
                            <% if (service.capabilities) { %>
                            <h6 class="fw-bold mb-2">核心能力：</h6>
                            <ul class="list-unstyled text-start">
                                <% service.capabilities.forEach(capability => { %>
                                <li class="mb-2">
                                    <i class="fas fa-cog text-primary me-2"></i><%= capability %>
                                </li>
                                <% }) %>
                            </ul>
                            <% } %>
                            
                            <% if (service.features) { %>
                            <h6 class="fw-bold mb-2">核心功能：</h6>
                            <ul class="list-unstyled text-start">
                                <% service.features.forEach(feature => { %>
                                <li class="mb-2">
                                    <i class="fas fa-check text-success me-2"></i><%= feature %>
                                </li>
                                <% }) %>
                            </ul>
                            <% } %>
                            
                            <% if (service.services) { %>
                            <h6 class="fw-bold mb-2">服务内容：</h6>
                            <ul class="list-unstyled text-start">
                                <% service.services.forEach(serviceItem => { %>
                                <li class="mb-2">
                                    <i class="fas fa-cloud text-info me-2"></i><%= serviceItem %>
                                </li>
                                <% }) %>
                            </ul>
                            <% } %>
                        </div>
                    </div>
                </div>
                <% }) %>
            </div>
        </div>
    </section>

    <!-- CTA区域 -->
    <section class="py-5 bg-primary text-white">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-8">
                    <h3 class="fw-bold mb-2">需要系统集成解决方案？</h3>
                    <p class="mb-0">联系我们的集成专家，为您定制专业的车载系统集成方案</p>
                </div>
                <div class="col-lg-4 text-end">
                    <a href="/contact" class="btn btn-warning btn-lg">
                        <i class="fas fa-comments me-2"></i>专家咨询
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- 页脚 -->
    <%- include('../partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>