# 本地开发环境配置
NODE_ENV=development

# 服务器配置
PORT=3001
HTTPS_PORT=3443

# 数据库配置 (使用Docker映射的外部端口)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=company_portal
DB_USER=postgres
DB_PASSWORD=company_portal_2025
DB_SSL=false

# Redis缓存配置 (使用Docker映射的外部端口)
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=company_portal_2025
REDIS_DB=0

# JWT配置
JWT_SECRET=company-portal-jwt-secret-dev-2025
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d